# Check len=1 trajectory should produce the same training results as original flowbot
def check_single_trajectory():
    # Run 10 epoch of original flowbot model

    # Run 10 epoch of 1-trajectory model

    # Compare the weights: r they the same (diff < eps)
    return
